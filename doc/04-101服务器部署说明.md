# 101 服务器部署说明（172.16.30.101）

> 部署方式：docker compose ｜ 部署目录：`/mnt/disk1/docker-data/qa-mini`
> 本文件是**实际部署记录**，任何变更（端口/目录/镜像/配置）请同步更新本文件
> （见 AGENTS.md §3）。

## 1. 部署信息

| 项 | 值 |
|---|---|
| 服务器 | 172.16.30.101（root 登录；CentOS Linux 8 Core x86_64） |
| Docker | Engine 26.1.3 + Compose v2.27.0 |
| 部署目录 | `/mnt/disk1/docker-data/qa-mini` |
| 数据目录 | `/mnt/disk1/docker-data/qa-mini/data`（挂载到容器 `/app/data`，SQLite 库所在） |
| 镜像 | `qa-mini:latest`（本地构建后 `docker save/load` 传入，见 §4 原因） |
| 端口 | **宿主机 8790 → 容器 8787**（部署前已探测 8788-8791 空闲，选 8790） |
| 首次部署日期 | 2026-09-20 00:59 |
| 访问地址 | Web 界面 / API：http://172.16.30.101:8790/ |
| 默认会话 | 会话ID `fa119a1d`（首次启动自动生成，名称「默认会话」） |

## 2. 目录结构（服务器侧）

```
/mnt/disk1/docker-data/qa-mini/
├─ server.js  package.json  README.md  .dockerignore
├─ lib/            config.js qa_runner.js sse.js protocols/
├─ public/         index.html app.js style.css
├─ docker/
│  ├─ Dockerfile
│  └─ docker-compose.yml     ← 端口 8790:8787
└─ data/           ← 运行时持久化（qa-mini.db / config.json）
```

## 3. 常用运维命令

所有 compose 命令在 `/mnt/disk1/docker-data/qa-mini/docker` 下执行：

```bash
cd /mnt/disk1/docker-data/qa-mini/docker

docker compose ps                     # 状态（应为 Up (healthy)）
docker compose logs -f --tail 100     # 日志
docker compose restart                # 重启
docker compose down                   # 停止（数据保留）
curl http://127.0.0.1:8790/api/health # 健康检查
docker compose exec qa-mini node -e "…"  # 容器内执行（如查库）
```

## 4. 镜像构建的特殊情况（重要）

101 的 Docker 配置了国内镜像源（daocloud/1ms/xuanyuan），其中 daocloud 对匿名
拉取返回 **401**，直接 `docker compose up -d --build` 失败。本机（Windows +
Docker Desktop）网络同样无法直连 Docker Hub，但可经 `docker.1ms.run` 拉取基础镜像。

**因此本部署采用「构建机出镜像 → 传输 → 加载」流程**（升级时也按此流程）：

```bash
# ① 构建机（有 Docker Desktop 的开发机）
docker pull docker.1ms.run/library/node:24-alpine   # 首次；已有本地 node:24-alpine 可跳过
docker tag  docker.1ms.run/library/node:24-alpine node:24-alpine
cd <qa-mini 项目根>
docker build -f docker/Dockerfile -t qa-mini:latest .
docker save qa-mini:latest -o /tmp/qa-mini.tar

# ② 传输到 101（scp / sftp / 任意方式）
scp /tmp/qa-mini.tar root@172.16.30.101:/tmp/

# ③ 101 上加载并启动
docker load -i /tmp/qa-mini.tar && rm -f /tmp/qa-mini.tar
cd /mnt/disk1/docker-data/qa-mini/docker
docker compose up -d          # 无 --build，直接用已加载的 qa-mini:latest
```

镜像含基础镜像层，tar 约 165MB。

## 5. 部署验证记录（2026-09-20 00:59）

| 验证项 | 结果 |
|---|---|
| `docker compose ps` | qa-mini `Up (healthy)`，端口 `0.0.0.0:8790->8787` |
| 容器内 `/api/health` | `{"ok":true,…,"sessions":1,…}` |
| 101 本机 `curl 127.0.0.1:8790/api/health` | ok |
| 构建机经局域网访问 `http://172.16.30.101:8790/api/health` | ok（外部可达） |
| 启动日志 | 生成 /app/config.json；默认会话 fa119a1d；SQLite 存储 /app/data/qa-mini.db |

## 6. 部署后待办（业务接入）

1. **配置后端协议**（已完成）：RAGFlow 已接入 `http://172.16.30.101:9380/api/v1`
   （chat `5199a490…`，API Key 由 RAGFlow 控制台获取），真实问答已验证通过。
   配置现落数据卷（compose 默认 `QA_MINI_CONFIG=/app/data/config.json`），
   容器重建/镜像升级不丢；DIFY 会话的后端尚未配置，需要时在「⚙ 设置」填写。
2. **创建 asr-tool 会话**：网页「会话列表」新建会话（或直接使用默认会话），
   在「会话设置」复制 asr-tool config.toml 片段（url 用
   `http://172.16.30.101:8790/api/push`），填入 asr-tool。
3. 防火墙/安全组放行 8790/tcp（已验证构建机可直连，说明当前策略放行）。

## 7. 升级流程（代码更新后）

1. 同步新代码到 `/mnt/disk1/docker-data/qa-mini`（覆盖 server.js/lib/public 等）；
2. 按 §4 流程在构建机重新 build + save + scp + load；
3. `cd …/docker && docker compose up -d`（换新镜像）；
4. `docker compose ps` + `curl …/api/health` 验证。

数据（data/ 卷）不受升级影响。

## 8. 升级记录

| 日期 | 内容 | 验证 |
|---|---|---|
| 2026-09-20 | 升级至 PWA + 滚动跟随版本（本地重建镜像 save/load，代码树同步）：manifest/sw.js（缓存 qa-mini-v2）/icons、空会话提示修复、流式滚动跟随 +「↓ 最新」浮钮、静态路由泛化 | 容器 Up (healthy)；health ok；数据卷会话保留（默认会话 fa119a1d + DIFY 5e0b7267）；manifest/sw.js/icons 均 200 且 MIME 正确；sw 版本 qa-mini-v2 |
| 2026-09-20 | 会话窗口打开即显最新位置（即时落底 + rAF 二次校正、移除 CSS smooth，SW v3）；**配置落数据卷**（compose 启用 `QA_MINI_CONFIG=/app/data/config.json`，现有 config 迁入 /app/data/），容器重建不再丢配置；RAGFlow 后端接入并真实问答验证 | 重建容器后 health ok、2 会话保留；ragflow url/chat_id/api_key 保留；真实问答 ok=true（115 字符）；sw 版本 qa-mini-v3 |
| 2026-09-20 | 生成时长统计（SW v4）：`records.finished_at`（旧库自动 ALTER 迁移）、历史/会话视图/done 事件带 `duration_s`；状态行显示耗时（「✔ 完成（732 字符 · 12.5s）」）、生成中实时计时 | 重建容器后 health ok、4 会话保留；ragflow 配置保留；真实记录含 duration_s（dify 13.7s / ragflow 2.9s）；sw 版本 qa-mini-v4 |
| 2026-09-20 | 会话列表按最新对话时间倒序（`updated_at` 降序，`GET /api/sessions` 与 SSE `sessions` 广播统一；前端无变更，SW 仍 v4） | 重建容器后 health ok、5 会话保留；ragflow 配置保留；列表验证：默认会话（04:51）在前、RAG（04:09）次之 |
| 2026-09-20 | 访问控制（SW v5）：右上角「会话设置/⚙ 设置」默认隐藏；`/admin` + 管理密码登录（首次输入即初始化，本机已初始化为约定初始密码，可在设置修改）；端点三级权限（公开/查看/管理）；非管理视图剥离会话 token；设置 → 安全 可关闭匿名访问并生成 6 位访问码（默认 8h、一键失效，暂未启用） | 重建容器后 health ok、7 会话保留、3 SSE 客户端在线；status admin_set false→true；config 无 token 401 / 带 token 200；匿名视图 0 个 token、管理视图可见；sw 版本 qa-mini-v5 |
| 2026-09-20 | 访问码管理优化（SW v6）：批量生成 1-10 个、每码独立时长、单个延期（renew）、清理全部过期码；设置 UI 状态徽标/剩余时长/复制/延期/失效 | 重建容器后 health ok、7 会话 + 3 SSE 客户端保留；管理密码跨重建保留（登录 initialized=false）；批量 3 码 + 延期 + 清理实测通过；sw 版本 qa-mini-v6 |
| 2026-09-20 | 安全页重新设计（SW v7）：卡片式三区块（匿名访问/访问码/管理密码）；匿名开关与修改管理密码即时生效（独立于「保存配置」）；生成表单带「码/有效时长（小时）/数量（个）」字段标签 | 重建容器后 health ok、7 会话 + 2 SSE 客户端保留；管理密码跨重建保留；新面板元素（sec-wrap/btn-save-adminpw/applyAnonymousToggle）均下发；sw 版本 qa-mini-v7 |
| 2026-09-20 | 设置抽屉重构（SW v8）：标签分「协议配置 / 安全配置」两组；抽屉加宽 880px、内容居中；标题「⚙ 设置」；修复移动端媒体查询丢失导致抽屉全屏的样式泄漏 | 重建容器后 health ok、7 会话 + 4 SSE 客户端保留；双组标签/880px/两个 640px 媒体块均下发；sw 版本 qa-mini-v8 |
| 2026-09-20 | 设置标签重设计（SW v9）：单行分段药丸标签（协议配置/安全配置 + 细线分隔），去掉表格感双行边框 | 重建容器后 health ok、7 会话 + 3 SSE 客户端保留；分隔线/药丸样式下发、无残留双行结构；sw 版本 qa-mini-v9 |
| 2026-09-20 | 答案字数改统计中文字数：状态行「完成（N 字）」（CJK 汉字，不含标点/字母/数字/换行）；服务端 countCJK，历史旧记录保留原「字符」文案 | 重建容器后 health ok、8 会话 + 3 SSE 客户端保留；真实 RAGFlow 问答实测 detail「完成（756 字）」、duration 10.6s |
| 2026-09-20 | 内容宽度三档（SW v10）：头部「宽度」下拉 窄 860px（默认）/ 宽 1180px / 铺满主容器，作用于问答卡片与底部输入框，localStorage 记忆 | 重建容器后 health ok、8 会话 + 3 SSE 客户端保留；width-mode/qa-w 变量/applyWidthMode 均下发；sw 版本 qa-mini-v10 |

## 9. 备份

- 全量备份 = 备份 `/mnt/disk1/docker-data/qa-mini/data/`（SQLite + 运行时配置）；
- 在线备份：`docker compose exec qa-mini node -e "…"` 或用 sqlite3
  `.backup` 后取出；
- 建议 crontab 每日备份并异地保存。
